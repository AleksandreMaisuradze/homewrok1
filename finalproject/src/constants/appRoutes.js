const Home = "/"
const Movies = "/movies"
const Registration = "/registration"
const LogIn = "/Login"
export const Movies_PDP = "/movies/:movieID"



const appRoutes = {
    Home, Movies, Registration, LogIn
}

export default appRoutes;